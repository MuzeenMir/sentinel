//! Block-page HTTP server bound to `127.0.0.1:80` (fallback `:8053`).
//!
//! Serves the calm, evidence-led block-page defined in `DESIGN.md` —
//! terminal-frame chrome, key/value props (feed, listed, ts, id),
//! `[Keep blocked] [Allow once] [Allow forever]` hierarchy.
