//! Tranco top-10k allowlist anchor.
//!
//! Refresh schedule: quarterly (T2 in `TODOS.md`). Atomic swap via
//! `tranco-baseline.txt.new` rename. Tray turns yellow if download fails
//! 3 consecutive times.
