apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sentinel.fullname" . }}-compliance-engine
  labels:
    {{- include "sentinel.labels" . | nindent 4 }}
    app.kubernetes.io/component: compliance-engine
spec:
  replicas: {{ .Values.complianceEngine.replicaCount }}
  selector:
    matchLabels:
      {{- include "sentinel.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: compliance-engine
  template:
    metadata:
      labels:
        {{- include "sentinel.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: compliance-engine
        app.kubernetes.io/part-of: sentinel
    spec:
      serviceAccountName: {{ include "sentinel.fullname" . }}-compliance-engine
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: compliance-engine
          image: {{ include "sentinel.image" (dict "service" .Values.complianceEngine "global" .Values) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.complianceEngine.port }}
              protocol: TCP
          env:
            - name: SERVICE_NAME
              value: "compliance-engine"
            {{- include "sentinel.commonEnv" . | nindent 12 }}
          resources:
            {{- toYaml .Values.complianceEngine.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel.fullname" . }}-compliance-engine
  labels:
    {{- include "sentinel.labels" . | nindent 4 }}
    app.kubernetes.io/component: compliance-engine
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.complianceEngine.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentinel.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: compliance-engine
