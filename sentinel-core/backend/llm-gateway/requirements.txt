Flask>=3.1.3
Flask-CORS>=6.0.0
requests>=2.31.0
redis>=5.0.1
gunicorn>=22.0.0
python-dotenv>=1.0.0
prometheus-client>=0.17.1
# get_node_alerts reads the local detector feed directly via _lib.db.
psycopg2-binary>=2.9.9

# LLM inference (lazy-imported in anthropic_client; only needed at runtime).
anthropic>=0.40.0

# OpenTelemetry — consistent with sibling services; no-op if endpoint unset.
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
opentelemetry-instrumentation-flask>=0.48b0
