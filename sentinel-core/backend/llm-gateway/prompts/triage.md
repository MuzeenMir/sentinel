The host intrusion detector raised an alert on this node. Triage it now.

Alert {{record_id}} — severity {{severity}}. Process "{{comm}}" ({{exe}}) on host {{hostname}}. Detector summary: {{summary}}.

Write 2–4 sentences covering: what happened, why it matters, and the recommended immediate action. Use ONLY the grounded data provided above — do not invent details. Cite the alert inline as [{{record_id}}] for every factual claim.
