The analyst asks a follow-up about entity `{{entity_id}}`:

"{{question}}"

Answer using only grounded data already gathered in this session plus any new
tool calls you make. Cite every fact with its record id. If the question cannot
be answered from available data, say what is missing. Stay advisory: propose,
never execute.
