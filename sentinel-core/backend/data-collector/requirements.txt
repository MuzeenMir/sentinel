Flask>=3.1.3
Flask-CORS>=6.0.0
kafka-python>=2.1.0,<3.0.0
redis>=5.0.1
requests>=2.31.0
psycopg2-binary>=2.9.9
prometheus-client>=0.17.1
gunicorn>=22.0.0  # WSGI server (was Docker-appended; pinned so the lockfile covers it)
gevent>=24.2.1  # gunicorn async worker (was Docker-appended; pinned so the lockfile covers it)
