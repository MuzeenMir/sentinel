# SENTINEL Environment
# Do not commit `sentinelenv` to git.

# JWT / Auth (required)
JWT_SECRET_KEY=Kjkn@^&52*4JD

# Initial admin (created on first auth-service start if none exists)
ADMIN_USERNAME=Santa
ADMIN_PASSWORD=Ggxr@123
ADMIN_EMAIL=santa@sentinel.local

# Database (docker-compose defaults; override if different)
POSTGRES_PASSWORD=sentinel_password
# DATABASE_URL=postgresql://sentinel:sentinel_password@postgres:5432/sentinel_db

# Redis
# REDIS_URL=redis://redis:6379

# Kafka
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092
