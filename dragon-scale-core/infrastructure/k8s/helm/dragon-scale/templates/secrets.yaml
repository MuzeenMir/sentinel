{{- if not .Values.vault.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: dragon-scale-db-credentials
  labels:
    {{- include "dragon-scale.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.postgresql.auth.password | required "postgresql.auth.password must be set" | quote }}
  connection-string: "postgresql://{{ .Values.postgresql.auth.username }}:{{ .Values.postgresql.auth.password }}@{{ include "dragon-scale.fullname" . }}-postgresql:5432/{{ .Values.postgresql.auth.database }}"
---
apiVersion: v1
kind: Secret
metadata:
  name: dragon-scale-auth-secrets
  labels:
    {{- include "dragon-scale.labels" . | nindent 4 }}
type: Opaque
stringData:
  jwt-secret-key: {{ .Values.auth.jwtSecretKey | required "auth.jwtSecretKey must be set (use --set or values file)" | quote }}
---
apiVersion: v1
kind: Secret
metadata:
  name: dragon-scale-redis-credentials
  labels:
    {{- include "dragon-scale.labels" . | nindent 4 }}
type: Opaque
stringData:
  redis-password: {{ .Values.redis.auth.password | default "" | quote }}
{{- end }}
{{- if .Values.vault.enabled }}
---
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: dragon-scale-vault-secrets
  labels:
    {{- include "dragon-scale.labels" . | nindent 4 }}
spec:
  provider: vault
  parameters:
    vaultAddress: {{ .Values.vault.address | quote }}
    roleName: {{ .Values.vault.role | quote }}
    objects: |
      - objectName: "jwt-secret-key"
        secretPath: {{ .Values.vault.secretPath | quote }}
        secretKey: "jwt_secret_key"
      - objectName: "db-password"
        secretPath: {{ .Values.vault.secretPath | quote }}
        secretKey: "db_password"
      - objectName: "redis-password"
        secretPath: {{ .Values.vault.secretPath | quote }}
        secretKey: "redis_password"
  secretObjects:
    - secretName: dragon-scale-auth-secrets
      type: Opaque
      data:
        - objectName: jwt-secret-key
          key: jwt-secret-key
    - secretName: dragon-scale-db-credentials
      type: Opaque
      data:
        - objectName: db-password
          key: password
{{- end }}
