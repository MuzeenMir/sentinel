# Kai Standup — 2026-06-03

## K1.1 API gateway Flask to FastAPI

- Built FastAPI `asgi_app.py` alongside the existing Flask `app.py`.
- Kept the existing Flask gateway parity suite green at each step.
- Added ASGI parity coverage for health/ready, auth/CORS/rate limit, auth proxy,
  threats, alerts, config, stats, SSE/error handling, policy, compliance, XAI,
  AI/DRL, hardening, HIDS, admin users, traffic, tenants, integrations, and SOC2
  audit endpoints.
- Flipped the API gateway container command to `uvicorn asgi_app:asgi`.
- Added ADR-016 documenting the runtime decision and the remaining Flask helper
  extraction follow-up.

## Verification

- `python -m pytest sentinel-core/backend/tests/test_api_gateway_asgi.py sentinel-core/backend/tests/test_api_gateway.py sentinel-core/backend/tests/test_container_runtime_deps.py -q`
- `ruff check` and `ruff format --check` on touched Python files
- `mypy --ignore-missing-imports --python-version 3.12 --no-incremental sentinel-core/backend/api-gateway`

## Follow-up

- Full Flask removal is not complete yet. `asgi_app.py` still imports selected
  helpers from Flask `app.py`; extracting or rewriting those helpers is required
  before claiming source-level Flask sunset.
