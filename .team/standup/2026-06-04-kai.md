# Kai Standup — 2026-06-04

## K1.1e API gateway Flask removal

- Extracted framework-agnostic gateway helpers into `gateway_core.py`; the ASGI
  application no longer imports the former Flask application.
- Added ASGI request-count and response-time middleware and Redis-backed global
  SlowAPI storage.
- Preserved verified SSE query-token identity when forwarding downstream and
  enforced admin plus tenant authorization on audit-read endpoints.
- Migrated the 131-test Flask oracle to the ASGI application without reducing
  coverage, then removed `api-gateway/app.py` and all gateway Flask dependencies.
- Made the shared audit logger's Flask request-context integration lazy so the
  Flask-free gateway image can import it.
- Updated ADR-016 to Accepted. Native asynchronous Redis SSE remains deferred.

## Verification

- Gateway, ASGI, container-runtime, and audit logger suites: `257 passed`.
- API gateway service-local health suite: `1 passed`.
- Ruff check and format checks passed on touched Python files.
- Mypy passed on `api-gateway/asgi_app.py` and `api-gateway/gateway_core.py`.
- Docker image built without Flask; live `/health` and `/readyz` smoke passed
  against Redis, and runtime inspection returned `flask_spec=None`.

## Honest claims

- Commit signing was unavailable because the configured temporary public key
  did not exist; the K1.1e removal commit is unsigned.
- Independent review has not yet been requested for this branch.
