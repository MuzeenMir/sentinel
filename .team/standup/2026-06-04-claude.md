# Standup — Claude (CLAUDE track) — 2026-06-04

**Branch:** `feat/phase-2-llm-analyst-copilot` (PR #56, draft). **Plan:** `.team/specs/2026-06-03-plan-CLAUDE-copilot-production-hardening.md`.

## Shipped (all TDD; backend 108 tests, frontend 127 tests; ruff/mypy/eslint/type-check clean)
- **C1** (`ac1221d`) — security-review fixes: `entity_id` allowlist (anti-SSRF/path-injection); rate-limit + tenant identity from gateway-authenticated request only (no spoof).
- **C4** (`ceee3c2`) — tamper-evident propose→confirm: HMAC-signed, single-use nonce, TTL; `/copilot/confirm` verifies + audits, never executes.
- **C2** (`05b16f6`) — verifiable citation provenance: cited id → `sha256(source)` + freshness; forged/stale rejected; provenance into result + audit + API.
- **C6** (`27a3fda`) — observability + audit completeness: prompt event (metadata only), every turn in ledger, cost/cache in answer; OTel spans (no-op safe).
- **C5** (`c941ee5`) — EU residency/provider seam + **ADR-021** (honest: routing seam, not shipped EU-resident inference).
- **C0** (`1d510ed`) — frontend HITL UI: CopilotPage + route + nav; CopilotPanel (summarize/ask/citations); ProposalCard + ConfirmDialog enforce no-auto-exec; copilot.ts service + confirm endpoint + C2/C4 fields.
- **C7** — **ADR-020** copilot trust & safety (this commit) with an honest shipped-vs-pending status table.

## Not shipped / gated (honest)
- **C1 red-team CI gate** (corpus + `llm-gateway-redteam.yml` + published residual) — NOT built.
- **C3 copilot RLS isolation** — gated on a real Marcus-reviewed migration (`audit-schema-guard`); sessions are Redis-only by design today.
- **Gateway `/copilot/*` proxy route** — the api-gateway must proxy to llm-gateway for the UI to reach the copilot in prod; not present yet.

## Owed before un-drafting PR #56
First real independent (different-model / Marcus) review pass of the copilot surface; then the C1 red-team gate + the gateway proxy route for true end-to-end.

## Cross-track (reviewed as marcus, claude-opus-4-8 ≠ Codex)
- PR #57 (OPA) → PASS; PR #58 (api-gateway FastAPI runtime) → PASS. Authored Kai prompt blocks: K1.1e (gateway Flask removal), K1.2 (auth-service), K3 (per-tenant DEK).
